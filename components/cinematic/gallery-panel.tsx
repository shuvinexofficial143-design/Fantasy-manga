"use client";
import {Download} from "lucide-react";
import type {CinematicImage} from "@/lib/types";
export function GalleryPanel({images,projectName,busy,onDownloadAll}:{images:CinematicImage[];projectName:string;busy:string;onDownloadAll:()=>void}){const ready=images.filter((image)=>image.image).sort((a,b)=>a.sceneNumber-b.sceneNumber),safe=(value:string)=>value.trim().replace(/[<>:"/\\|?*\u0000-\u001F]/g,"-").replace(/\s+/g," ").slice(0,70)||"cinematic",download=(image:CinematicImage)=>{if(!image.image)return;const a=document.createElement("a");a.href=image.image;a.download=`${safe(projectName)}-scene-${String(image.sceneNumber).padStart(3,"0")}.jpg`;a.click()};return <section className="space-y-4"><div className="flex flex-col gap-3 rounded-2xl border border-slate-200 bg-white p-4 shadow-sm sm:flex-row sm:items-center sm:justify-between"><div><div className="font-bold">Generated Sequence</div><div className="text-sm text-slate-500">{ready.length}/{images.length} images ready in story order.</div></div><button disabled={!!busy||!ready.length} onClick={onDownloadAll} className="rounded-xl bg-slate-900 px-4 py-2 text-sm font-bold text-white disabled:opacity-50"><Download className="mr-1 inline" size={15}/> Download All</button></div><div className="grid gap-4 md:grid-cols-2 xl:grid-cols-3">{ready.map((image)=><article key={image.id} className="overflow-hidden rounded-2xl border border-slate-200 bg-white shadow-sm"><img src={image.image} alt={image.title} className="aspect-video w-full object-cover"/><div className="p-4"><div className="text-xs font-bold uppercase tracking-wider text-violet-600">Scene {image.sceneNumber}</div><div className="mt-1 font-bold">{image.title}</div><div className="mt-2 text-xs text-slate-500">{image.provider||"provider"} · {image.model||"model"} · {image.referenceCount||0} refs</div><button onClick={()=>download(image)} className="mt-3 rounded-lg border border-slate-200 px-3 py-2 text-xs font-semibold text-slate-700"><Download className="mr-1 inline" size={13}/> Download</button></div></article>)}{!ready.length&&<div className="rounded-2xl border border-slate-200 bg-white p-10 text-slate-500">Continuity Scenes tab से images generate करें।</div>}</div></section>}
